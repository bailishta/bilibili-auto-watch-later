# 更新日志

## 2.0.0 — 2026-09-13

本版本仅发布 Chrome 插件，Android 客户端未更新。

### 新增

- 独立管理中心：名单、排程、更新分布与导入导出集中在单独标签页，弹窗保留状态、统计和基本操作。
- 多个自动检查器：独立名称、启停、每周每天时间、视频时间窗口及全部／指定 UP 主范围；同时触发时排队执行。
- 更新分布面板：查看自然周及历史周，进入某天默认显示上午，可切换下午。无指针的 12 小时表盘配合分钟刻度与更新标记，具体时间、人数、头像和名字直接显示在紧凑列表中。
- 自定义保留最近 1–52 个自然周的更新记录，默认最近 3 周；记录来自日常检查采集。
- 配置 JSON 备份与恢复：包含检查器、设置、追踪名单及已采集的更新记录；导入前校验并预览，确认后替换配置、重建排程。

### 调整与修复

- 名单排序使用粉丝数和最后一次视频发布时间，自动／手动检查时统一采集并持久化，切换排序无需重新请求数据。
- 移除手动「刷新数据」按钮；请求失败时保留已有排序数据。
- 修复检查器保存时无响应的问题，提供明确的输入校验和错误提示。
- 使用接近 B站的配色和系统字体，弹窗、管理页适配深色模式及窄屏。
- 简化管理入口与统计面板文案，增加非线性过渡，并遵循系统减少动态效果设置。
- 保留名单 JSON 导入／导出兼容性；旧排程自动迁移为默认检查器。

### 升级说明

- 下载 `bilibili-auto-watch-later-v2.0.0.zip`，沿用原扩展目录替换文件，在扩展管理页重新加载并刷新管理页。
- 配置备份不包含登录 Cookie；恢复会替换当前检查器、设置和名单，保留本机已处理视频记录及统计。
- 更新分布不是完整投稿历史，每次检查仅积累接口返回的近期视频，每位 UP 主最多保留 500 条记录。
- Android APK 仍在 [v1.4.0](https://github.com/bailishta/bilibili-auto-watch-later/releases/tag/v1.4.0) 下载。

## 1.4.0 — 2026-07-16

- 新增追踪名单 JSON 导入／导出，支持 Chrome 插件与 Android 客户端互通。
- Chrome 插件使用 downloads API 导出名单，修复弹窗中的下载问题。
